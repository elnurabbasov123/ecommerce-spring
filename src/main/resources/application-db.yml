spring:
  datasource:

    driver-class-name: org.postgresql.Driver
    password: 123456
    username: postgres
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=ecommerce-spring
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true